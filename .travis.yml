sudo: required

language: java
script: mvn -q install --settings .travis-settings.xml
jdk:
  - oraclejdk7
  
after_script:
  - docker build -t dbmaintain/dbmaintain .
#branches:
#  only:
#  - master
#  - develop

services:
  - docker
